# 語彙・語句コンテンツ作成ルール

この文書は、単語・コロケーション・イディオム・例文を追加するときの基準です。

## 件数の考え方

- `500 WORDS` は、4つの vocabulary JSON に登録された500個の単語エントリーを指す。
- `src/data/phraseCatalog.ts` のコロケーション／イディオム60問は、500語とは別のカタログである。
- したがって、学習対象を単純に数える場合は「単語500語＋語句問題60問」と数える。ただし、辞書の件数表示は単語500語だけを表示する。
- 語句問題は、関連する単語の辞書カードを開くと `PHRASES IN QUIZ` として確認できる。

## 単語データ

単語は `VocabularyEntry` の形式で登録する。

- `id` は一意にする。
- `lemma` は見出し語の基本形にする。重複する見出し語は登録しない。
- `pronunciation` は米国英語を優先した発音記号を登録する。未登録の場合も、`pronunciationCatalog` で必ず補完できる状態にする。
- `level` は1〜5で、学習難度を表す。
- `senses` には実際に学習させたい品詞と日本語訳を登録する。主要な意味を先頭に置く。
- 英語定義は辞書的で短い自然な英文にする。見出し語そのものを使った循環的な定義や、意味を答えにしてしまう定義は避ける。

## 例文データ

単語の例文は `examples` 配列に1つ以上登録できる。現在の型はすでに複数例文に対応している。

各例文の必須条件は次のとおり。

- 英文は、その単語が実際にその意味・品詞で使われる自然な文にする。
- 日本語訳は英文全体の自然な訳にする。単語の意味を括弧で挿入するだけの訳は使わない。
- 例文ごとに異なる場面・主語・用法を選び、単なる語句の置換で増やさない。
- `clozeTarget` には英文中に実際に現れる形を登録する。基本形だけでなく、`identified`、`depends`、`modified` のような活用形も登録できる。
- 追加例文がある場合、`examples` に追加する。問題生成時は有効な例文から1つを選び、回答後は配列内の全例文を表示する。
- 例文が空、英文と日本語訳の対応が不自然、または見出し語が英文に存在しない場合は採用しない。

### 空所補充のルール

- 空所補充は、選んだ例文中の解答語を必ず `_____` に置き換える。
- 解答語が活用形で文中に現れる場合も、選択肢の正解は見出し語を使う。例：`modify` → 文中の `modified`。
- 解答語の直前に不定冠詞 `a` または `an` がある場合、冠詞も含めて空欄にする。選択肢も冠詞込みにする。

  例：`We need an alternative solution.`

  - 出題文：`We need _____ solution.`
  - 正解選択肢：`an alternative`
  - 他の選択肢も、必要に応じて `a fundamental` のように冠詞込みにする。

- 冠詞を含めることで文法的に明らかな選択肢を作らない。冠詞を含める処理ができない例文は、別の例文に差し替える。
- すべての空所補充問題は、生成後に `_____` が1つ以上残っていることを検証する。

## コロケーション／イディオム

- 語句問題は単語500語とは別に `phraseCatalog` で管理する。
- `expression` は完成した語句を記録する。たとえば `look at`、`take part in`、`break down`。
- 空欄には、語句の一部である前置詞・副詞などを置く。解答後は、空欄の語だけでなく完成した表現も表示する。
- 各問題は4つの異なる選択肢を持ち、正解以外の選択肢にもその語句としての意味を付ける。
- 語句全体の日本語訳と英語説明は、単語単体の意味ではなく、その語句の用法を説明する。
- 例文を追加する場合は `phraseExampleCatalog.ts` に問題IDごとに登録する。各追加例文には自然な英文・日本語訳を登録し、必要ならその例文専用の `prompt` も登録する。
- 語句の複数例文も、出題時は1つを選び、回答後はすべて表示する。

## クイズ表示と音声

- 回答前は、日本語の選択肢の訳を表示しない。英単語4択問題でも、回答後に初めて日本語訳を表示する。
- 回答前は発音ボタンを表示しない。回答後に、英単語・英文・英語定義の発音を表示する。
- 日本語の選択肢には発音ボタンを付けない。
- 音声は `en-US` を優先し、英語音声が見つからない場合に他言語の音声へフォールバックしない。
- 日本語訳、英語定義、例文の日本語訳を混ぜた仮の英文を、実例文として使わない。

## 追加・レビュー手順

1. 単語なら vocabulary JSON、語句なら phrase カタログまたは phrase example カタログに登録する。
2. 発音記号、品詞、日本語訳、英語定義、例文とその日本語訳をそろえる。
3. 空所補充なら `clozeTarget`、活用形、冠詞、句読点を確認する。
4. 辞書で検索・展開し、語句と複数例文が表示されることを確認する。
5. `pnpm.cmd run test` と `pnpm.cmd run build` を実行する。
6. 例文は、文法・語法・日本語訳の自然さを人間が最終確認する。自動生成だけで採用しない。

## 自動チェックの最低条件

- 単語数が500（増量後は目標数）で、見出し語が重複しない。
- すべての単語に発音記号と英語定義がある。
- 古い仮テンプレートや、見出し語を不自然に目的語にした英文がない。
- 空所補充の全問題に `_____` がある。
- 語句IDが一意で、各語句問題に4選択肢がある。
- テストと本番ビルドが成功する。
