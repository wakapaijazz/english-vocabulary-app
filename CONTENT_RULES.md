# 語彙・語句コンテンツ作成ルール

この文書は、単語・コロケーション・イディオム・例文を追加・修正するときの基準です。アプリの表示、クイズ生成、音声、辞書の件数表示と一致させます。追加・変更のたびに、この文書全体を確認してから作業します。

## 1. 件数とデータの責務

- 単語は次の5つのJSONを統合して管理する。`vocabulary.json`、`vocabulary-extra.json`、`vocabulary-more.json`、`vocabulary-final.json`、`vocabulary-expansion.json`。現在は合計700語。
- 現在の700語は共通テストレベルの基礎・標準語彙として、原則 `common-test` タグを付ける。件数を増やすためだけに、出題価値の低い語を追加しない。
- 語句問題は `phraseCatalog.ts` と `phraseCatalogExpansion.ts` を `allPhraseCatalog` で統合して管理する。現在は単語700語とは別に100問。
- 辞書には「単語」「語句」の2タブがあり、語句タブで100問すべてを直接確認できる。語句を単語カード内だけに頼らない。
- 単語の例文は `src/data/exampleResolver.ts` の `getWordExamples()` で統合する。画面やクイズ側で個別に配列を組み立てない。
  - JSONの `entry.examples`: 各単語の基本例文。
  - `exampleCatalog.ts`: 旧データに直接例文がない単語を補う旧例文。
  - `exampleRevisionCatalog.ts` / `exampleRevisionExtra.ts`: 既存の先頭例文を、より自然で別の用法の例文へ差し替える編集レイヤー。
  - `exampleSupplementA.ts`〜`E.ts`: 既存500語のレビュー済み追加例文。
  - `exampleThirdCatalog.ts`: 複数の意味・用法がある既存語の3つ目の例文。
- 語句の例文は `getPhraseExamples()` で統合する。
  - `phraseCatalog.ts` / `phraseCatalogExpansion.ts`: 基本例文。
  - `phraseExampleCatalog.ts` / `phraseExampleExpansion.ts`: 追加例文。
  - `phraseExampleRevision.ts`: 基本例文と同じ文になった追加例文を別用例へ差し替える編集レイヤー。

## 2. 単語データ

`VocabularyEntry` の必須確認項目は次のとおり。

- `id` と `lemma` はそれぞれ一意にする。`lemma` は見出し語の基本形にする。
- `level` は1〜5。共通テスト語彙には `common-test`、将来の上位語彙には試験区分を表すタグを付ける。
- `senses[0]` は主要な品詞・日本語訳にする。英語定義は短く自然な辞書的英文にし、見出し語を循環的に説明しない。
- 発音記号は米国英語を優先する。JSONにない場合は `pronunciationCatalog.ts` に追加し、アプリ起動後の全単語に発音記号がある状態にする。
- コロケーション／イディオムを単語側にも登録する場合、表現・意味・例文の主語と用法が自然であることを確認する。
- 多義語・品詞違いがある単語は `senses` に用法を分ける。`senses.length > 1` の単語は最低3例文にする。

### 今後のレベル拡張ルール（今回追加した運用案）

- 700語の共通テスト土台と、将来追加する国公立二次・英検準1級・英検1級レベルを混ぜて管理しない。上位語彙には `advanced-secondary`、`eiken-pre1`、`eiken-1` など、出典・対象レベルが分かるタグを付ける。
- 同じ語の難しい意味を追加するときは、既存の `senses` に追加し、例文をその意味専用にする。単語を重複登録しない。
- 新しい試験レベルを追加するときは、件数だけでなく、抽象語・学術語・社会問題・自然科学・読解頻出語などの分野の偏りを点検する。
- 発音記号・品詞・英語定義・日本語訳・例文がそろわない語は、登録件数に含めない。

## 3. 例文

### 必須条件

- `getWordExamples(entry)` の結果が、すべての単語で最低2例文になるようにする。
- 複数の意味・品詞・一般的な使われ方がある単語は最低3例文にする。例文ごとに意味・品詞・場面のどれを示すかを確認する。
- 英文は対象語が実際の意味・品詞で使われる自然な文にする。
- 日本語は英文全体の自然な訳にする。単語の意味を括弧で挿入するだけの訳は禁止する。
- `clozeTarget` を持つ例文では、その活用形が英文中に実際に現れること。例：`identify` に対して `identified`。
- `exampleCatalog` 由来の旧例文は `clozeTarget` を持たない場合があるため、クイズ生成側が基本形・活用形を検出する。新規例文には可能な限り `clozeTarget` を明示する。
- `The word "..." is useful in context.`、`This example shows how to use ...` などの仮テンプレートは禁止する。
- 見出し語を不自然な目的語に置いただけの文は禁止する。単語固有の意味・用法が分かる文にする。
- ことわざ、名言、定着した比喩表現がその語の意味・用法を自然に示す場合は、優先して採用する。ただし、出典不明の引用を無理に作らない。

### 複数例文の差別化

- 例文1・例文2は、単語だけを置き換えたり、後ろに説明を足しただけにしない。
- 主語、場面、対象、時間、場所、結果、または意味・品詞の少なくとも複数を変える。
- 例：次の組み合わせは不可。

  - `The campaign targets young adults.`
  - `The campaign targets young drivers who are new to the city.`

- 上記のような場合は、詳しい方を残し、短い方を別場面の文に差し替える。
- 例文を追加したら、辞書では全例文、クイズ回答後では全例文が表示されることを確認する。表示名は `例文1`、`例文2`、`例文3` の形式にする。

### 空所補充

- 生成後の出題文には `_____` がちょうど1つ残ること。
- `clozeTarget` が活用形の場合、表示上はその活用形を空欄にするが、正解選択肢は見出し語を使う。
- 解答語の直前に `a` / `an` がある場合は、不定冠詞も空欄に含める。

  - 例文：`We need an alternative solution.`
  - 出題文：`We need _____ solution.`
  - 正解：`an alternative`

- a/anを含める処理で他の選択肢が文法的に明らかになる場合は、例文を変更するか、選択肢にも正しい冠詞を付ける。
- 空欄がない、複数ある、対象語が見つからない例文は採用しない。

## 4. コロケーション／イディオム

- 各IDは一意にする。基本60問は `phraseCatalog.ts`、追加40問は `phraseCatalogExpansion.ts` に登録し、出題・辞書では `allPhraseCatalog` を使う。
- 各問題は完成した `expression`、関連語 `word`、日本語訳、英語説明、例文を持つ。
- `prompt` は空欄をちょうど1つ含める。`answer` は完成した表現の空欄部分であること。
- 選択肢は4つ、テキストはすべて異なるものにする。複数語の答えも可とする（例：`down on`）。
- `phraseMeaningCatalog.ts` と `phraseMeaningExpansion.ts` に4選択肢すべての意味を登録する。正解以外も、完成した語句としての意味、または「この文では意味をなさない」と明示する。
- 例文の日本語訳は、語句の意味を括弧で足すのではなく、文全体を自然に訳す。
- 追加例文は `phraseExampleCatalog.ts` / `phraseExampleExpansion.ts` に登録し、英文・日本語訳・必要なら専用の `prompt` を持たせる。出題時は1つを選び、回答後と辞書展開後はすべて表示する。
- 語句の例文も、主語・場面・用法を変え、元の例文の単なる長文化や再掲にしない。
- 語句の分類は、前置詞を伴う定型表現、句動詞、慣用表現、一般的なコロケーションを含む。単なる任意の単語の組み合わせは登録しない。

## 5. クイズ・辞書・音声の表示仕様

- 辞書は初期状態では単語または語句の見出しだけを表示する。展開ボタンで発音記号、品詞、意味、定義、例文、日本語訳、音声を表示する。
- 単語カードは `.compact` で表示される場合がある。詳細を追加・変更するときは、`.compact .word-details` のCSSで詳細が隠れないことを確認する。
- クイズ回答前は、日本語の選択肢訳と、問題・選択肢・英文の発音ボタンを表示しない。
- 回答後は、英単語・英文・英語定義の発音を表示する。日本語は読み上げない。
- 音声は `en-US` を優先し、英語音声がない場合に他言語へフォールバックしない。
- クイズ解説の例文は、英文と日本語訳を別行にし、`例文1`、`例文2` のように番号を付ける。
- お気に入りは単語ID、語句は `phrase:<phraseId>` の形式で保存する。

## 6. 追加・レビュー手順

1. この文書を最初から最後まで読む。
2. 単語は適切な vocabulary JSON、語句は基本または拡張カタログに登録する。
3. 品詞、日本語訳、英語定義、発音記号、最低2例文をそろえる。多義語は最低3例文にする。
4. 例文ごとに自然な日本語訳、`clozeTarget`、活用形、冠詞、句読点を確認する。
5. 例文同士が別場面・別用法になっているか確認する。長い文を短い文に足しただけなら作り直す。
6. 語句は4選択肢の意味、空欄1つ、追加例文、語句の分類を確認する。
7. 辞書の単語タブ・語句タブで、折りたたみ状態と展開状態を確認する。
8. クイズで回答前／回答後の訳・音声・例文表示を確認する。
9. `pnpm.cmd run test` と `pnpm.cmd run build` を実行する。
10. 問題がなければ、件数・データ構造・ルール文書を同じコミットに含める。

## 7. 自動チェック

テストでは少なくとも次を検証する。

- 単語700語、ID・見出し語の重複なし。
- 700語すべてに発音記号と英語定義がある。
- 700語すべての統合済み例文が2つ以上あり、複数意味語は3つ以上ある。
- 例文に空欄・日本語訳不足・仮テンプレートの問題がない。
- 例文ペアの内容語がほぼ同じになるケースを検出する。
- 100語句、語句ID、空欄1つ、4選択肢、選択肢意味、追加例文を検証する。
- クイズ生成後の全空所補充問題に `_____` がある。
- 語句の追加例文が基本例文と同一でないことを検証する。
- TypeScriptの型チェックと本番ビルドが成功する。

自然さ、語法、意味の適切さは自動テストだけでは保証できないため、最終的に人間が英文と日本語訳を読む。

## 8. 追加時に提案するルール

- 追加語彙には、試験レベルだけでなく出典・分野タグを付け、将来「共通テスト」「二次試験」「英検準1級」「英検1級」を別々に絞り込めるようにする。
- 単語と語句の件数表示は、データファイルの数値を直接書かず、統合後の配列長から表示する。今回の700／100対応でもこの方式を維持する。
- 例文作成時は、追加前に既存例文との内容語・主語・構造を比較し、追加後に自動類似度テストを実行する。
- 例文や語句を大きく追加する場合は、今回のように「基本データ」「追加データ」「編集・修正レイヤー」「共通Resolver」「自動テスト」を分離し、画面ごとの個別実装を増やさない。
