import { useState } from "react";
import type { VocabularyEntry } from "../types/vocabulary";
import { POS_LABELS } from "../types/vocabulary";
export function WordCard({entry,compact=false}:{entry:VocabularyEntry;compact?:boolean}){ const [expanded,setExpanded]=useState(!compact); const mainSense=entry.senses[0]; return <article className={`word-card ${compact?"compact":""}`}><div className="word-card-top"><div><div className="word-meta"><span className="level-pill">LEVEL {entry.level}</span><span>{POS_LABELS[mainSense.partOfSpeech]}</span></div><h3>{entry.lemma}</h3>{entry.pronunciation&&<p className="pronunciation">{entry.pronunciation}</p>}</div><button className="icon-button" aria-label={expanded?"詳細を閉じる":"詳細を表示"} onClick={()=>setExpanded(value=>!value)}>{expanded?"−":"+"}</button></div><div className="sense-list">{entry.senses.map((sense,index)=><div className="sense-row" key={sense.id}><span>{index+1}</span><p>{sense.meaningJa}</p></div>)}</div>{expanded&&<div className="word-details">{entry.collocations?.length?<div className="detail-group"><span className="detail-label">COLLOCATIONS</span>{entry.collocations.map(item=><p key={item.id}><strong>{item.expression}</strong>{item.meaningJa&&<em>{item.meaningJa}</em>}</p>)}</div>:null}{entry.idioms?.length?<div className="detail-group"><span className="detail-label">IDIOM</span>{entry.idioms.map(item=><p key={item.id}><strong>{item.expression}</strong><em>{item.meaningJa}</em></p>)}</div>:null}{entry.derivatives?.length||entry.synonyms?.length?<div className="detail-group"><span className="detail-label">RELATED WORDS</span><p className="tag-list">{[...(entry.derivatives??[]),...(entry.synonyms??[])].map(word=><span key={word.word}>{word.word}</span>)}</p></div>:null}{entry.examples?.[0]&&<div className="example-box"><span className="detail-label">EXAMPLE</span><p className="example-en">{entry.examples[0].english}</p><p className="example-ja">{entry.examples[0].japanese}</p></div>}</div>}</article>; }
